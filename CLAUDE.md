@AGENTS.md

## Claude Code notes

The project brief above (`AGENTS.md`) is the single source of truth — shared with Cursor and any other agent. Don't hand-duplicate its tables (skills, architecture, docs index) elsewhere; edit `AGENTS.md` and this file picks it up automatically via the `@AGENTS.md` import.
