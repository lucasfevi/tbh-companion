/** Main companion window — fixed width for Inventory table + summary cards. */
export const MAIN_WINDOW_WIDTH = 900;
export const MAIN_WINDOW_HEIGHT = 640;
export const MAIN_WINDOW_MIN_HEIGHT = 480;
